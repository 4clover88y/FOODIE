<div class="container">
    <% if(user.mem_id) { %>
        <h1 id="chat-head">CHAT</h1>
        <div id="info">
            <table>
                <tr>
                    <th><img src="../images/person.png" alt="현재 접속 인원: " style="margin: 3px; width: 15px;height: 15px;"></th>
                    <th><strong id="count"></strong></th>
                </tr>
            </table>
        </div>
        <div id="chat"></div>
        수정 - action = "/chat/<%= roomId %>" 없앰
        <form id="chatForm" method = "POST">
            <input id="chat-input" type="text" required>
            <input id="chat-mem-id" value="<%= user.mem_id %>">
            <input id="chat-gathering-id" value="<%= roomId %>">
            <input type="submit" class="button" value="Send">
        </form>
        <script type="text/javascript" src="/js/testChat.js"></script>
        <%= user.mem_id %><br>
        
        <%= roomId %>는 룸 아이디야
    <% } %>
</div>