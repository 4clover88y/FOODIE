db 검색해서 소속된 그룹 번호 찾고 그 그룹들만큼 for문 돌려. 그리고 
<form action="/chat/list" method="post">
    <input type="text" name="roomId" value="1" readonly>
    1번 방 <button type="submit">입장하기</button>
</form>
<form action="/chat/list" method="post">
    <input type="text" name="roomId" value="2" readonly>
    2번 방<button type="submit">입장하기</button>
</form>
DATA가 DB에서 검색하고 없으면 NON -CHAT LIST 띄위ㅓ.
<div id="selectRoom">
    스크립트에서 form 태그 동적 생성 
    <form action="/chat/list" method="POST">
        <input type="text" name ="roomId" value="2" hidden>
        <button type="submit" class="roomId" value="2">입장하기</button>
        roomId = 디비에서 가져온 gathering_id<br>
        action="/chat/roomId"<br>
        value=roomId<br>
    </form> 

    <script>
        // const buttons = document.querySelectorAll('.roomId');
        // buttons.forEach(button=>{
        //     button.addEventListener('click', ()=>{
        //         alert(`${button.value} is clicked`);
        //     })
        // })
    </script>
</div>
